(ns architect.main)

(defn initGame [game]
  (
    ;set csrf ttownid etc to bt state


    ))

(defn startFarm [state]
  (
    ;if buff then double farm
    ;if almost cap remove vilage from farm list til reset
    ;if ress cap stop farm
    ))

(defn spendRes [state]
  (
    ; if free nass > 200 or free nass > 10%
    ; and has free slot
    ; wait for almost res cap
    ; cast spel
    ; build army
    ; else
    ; if has free build slot
    ; build bilding by template
    ; if cannot build build farm
    ))

(defn prepareToAttack [state]
  (
    ; send leter to defers with income times
    ; save atack army till 3 min to attack
    ;
    )
  )

(defn sendAtack [state]
  (
    ; if has task to destroy and
    ))

(defn farmTown [data]
  (
    ;set farm list
    ))

(defn initData [srvData]
  (
    ;init farm
    ))

(defn startBot [state]
  (
    ; start Farm
    ))



(-> state
    (initGame)
    (initData)
    (startBot))
